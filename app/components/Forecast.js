var React = require('react');

class Forecast extends React.Component {
  render() {
    return (
      <div>
        This is the forecast component
      </div>
    );
  }
}

module.exports = Forecast;